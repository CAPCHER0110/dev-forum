events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # 配置 Docker 内部 DNS 解析器
    # 127.0.0.11 是 Docker 容器内部的 DNS 服务器地址
    # valid=30s 表示解析结果缓存 30 秒
    resolver 127.0.0.11 valid=30s;

    # 1. 前端转发
    server {
        listen 80;
        server_name forum.local;
        
        # 定义变量
        set $upstream_web "http://web:3000";

        location / {
            # 🔥 使用变量 proxy_pass，强制 Nginx 运行时解析，而不是启动时解析
            proxy_pass $upstream_web;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    # 2. 后端 API 转发
    server {
        listen 80;
        server_name api.forum.local;

        # 定义变量，把 api:4000 赋值给它
        set $upstream_api "http://api:4000";

        location / {
            # 使用变量，这样 Nginx 启动时如果 api 不存在也不会报错
            proxy_pass $upstream_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}